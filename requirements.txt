# 道路裂缝检测项目依赖
# Road Damage Detection Dependencies

# YOLO 核心库
ultralytics>=8.0.0

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0

# 数据处理
numpy>=1.23.0
opencv-python>=4.8.0
Pillow>=10.0.0

# 配置文件
PyYAML>=6.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 文本编码 (推荐安装, 不装则用内置 fallback 编码器)
sentence-transformers>=2.2.0

# 拓扑计算 (推荐安装, 不装则用纯 PyTorch 近似)
# gudhi>=3.8.0
# giotto-tda>=0.6.0

# 进度条
tqdm>=4.65.0

# 阿里云 OSS (可选，用于上传数据)
oss2>=2.18.0

# 日志
tensorboard>=2.13.0

